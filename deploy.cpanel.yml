---
deployment:
  tasks:
    - export DEPLOYPATH=/docs.contactit.fr/
    - /bin/cp -R categories $DEPLOYPATH
    - /bin/cp -R liens_utiles $DEPLOYPATH
    - /bin/cp -R linux $DEPLOYPATH
    - /bin/cp -R mentions_legales $DEPLOYPATH
    - /bin/cp -R ressources $DEPLOYPATH
    - /bin/cp -R tags $DEPLOYPATH
    - /bin/cp -R Reseaux $DEPLOYPATH
    - /bin/cp -R Windows_Server_LDAP $DEPLOYPATH
    - /bin/cp deploy.cpanel.yml $DEPLOYPATH
    - /bin/cp index.html $DEPLOYPATH
      /bin/cp 404.html $DEPLOYPATH

